/*Instruction during the lab*/

#include <stdio.h>
#include <math.h>

int main(void)
{
	float f = 3.14, squared = 9.0;

	printf("pi is %f and delicious\n", f);
	
	printf("the square root of squared is %f\n", sqrt(squared));

	return 0;
}
